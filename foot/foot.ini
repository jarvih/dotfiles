[main]
font=monospace:size=12

# Allow pixel-precise window resizing instead of snapping to cell multiples
resize-by-cells=no
# Reflow immediately while dragging instead of after 100ms of idle
resize-delay-ms=0
